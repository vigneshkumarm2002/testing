/** @type {import('next').NextConfig} */

const nextConfig = {
  reactStrictMode: true,
  output: 'export',
  distDir: 'out', // Change 'out' to your desired output folder
}

module.exports = nextConfig
